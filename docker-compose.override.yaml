services: # Desarrolo.
  app:
    build:
      target: dev
    volumes:
      - .:/app
    environment:
      GIN_MODE: debug

  db:
    volumes:
      # Base de datos utilizada por atlas.
      - ./database/init:/docker-entrypoint-initdb.d
