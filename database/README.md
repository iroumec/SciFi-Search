# Importante

No modificar los directorios `init` ni `migrations` (utilziado por Atlas, mantiene el versionado de los cambios que se fueron realizando).

Si se desea realizar modificaciones a la base de datos, modificar únicamente los directorios `queries` y `schema`.
