package views

import sqlc "scifi-search/app/database"

// ------------------------------------------------------------------------------------------------

// ------------------------------------------------------------------------------------------------
templ indexContent(list []sqlc.User) {
	<main class="index-content">
		@firstQuery()
	</main>
}

// ------------------------------------------------------------------------------------------------
templ IndexPage(list []sqlc.User, isUserAuthenticated bool) {
	@Layout("SciFi Search", isUserAuthenticated, func() templ.Component {
		return indexContent(list)
	})
}

// ------------------------------------------------------------------------------------------------
