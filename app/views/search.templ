package views

import "scifi-search/app/utils"

// ------------------------------------------------------------------------------------------------
// Imports
// ------------------------------------------------------------------------------------------------

// ------------------------------------------------------------------------------------------------
// Search Results Content
// ------------------------------------------------------------------------------------------------
templ searchResultsContent(query string, hits []map[string]any) {
	<div class="search-container-grid">
		<div class="filter-section">
			Filtros
		</div>
		<div class="results-section">
			<div class="results">
				if len(hits) == 0 {
					<p>No se encontraron resultados.</p>
				} else {
					<h1>Resultados de búsqueda para “{ query }”</h1>
					for _, m := range hits {
						<div class="result">
							<a id="title" href={ utils.ToString(m["Link"]) }>{ utils.ToString(m["Nombre"]) }</a>
							<p id="extra-info">
								{ utils.ToString(m["Tipo"]) }
								{ utils.ToString(m["Gran area 1"]) }
								{ utils.ToString(m["Gran area 2"]) }
							</p>
							<p id="description">{ utils.ToString(m["Descripcion"]) } </p>
						</div>
					}
				}
			</div>
		</div>
	</div>
}

// ------------------------------------------------------------------------------------------------
// Search Results Page
// ------------------------------------------------------------------------------------------------
templ SearchResultsPage(query string, hits []map[string]any, isUserAuthenticated bool) {
	@Layout("Resultados para "+query, isUserAuthenticated, func() templ.Component {
		return searchResultsContent(query, hits)
	})
}

// ------------------------------------------------------------------------------------------------
